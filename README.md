# how to run

```
$ funktion create fn -f ucase.js
$ funktion create fn -f reverse.js
$ funktion create fn -f test.js
$ kubectl create -f process-svc.yaml
$ funktion create flow -n process -c http4 http://localhost http://ucase
```


